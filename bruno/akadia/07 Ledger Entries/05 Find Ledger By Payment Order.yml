info:
  name: 05 Find Ledger By Payment Order
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/payment-orders/:id/ledger"
  params:
    - name: id
      value: "{{ledger_payment_order_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
