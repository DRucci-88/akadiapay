info:
  name: 06 Manual Post Ledger - Idempotent Retry
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/payment-orders/:id/post-ledger"
  params:
    - name: id
      value: "{{ledger_payment_order_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
