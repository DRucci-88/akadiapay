info:
  name: 05 Find By ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/payment-products/:id"
  params:
    - name: id
      value: "{{payment_product_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
