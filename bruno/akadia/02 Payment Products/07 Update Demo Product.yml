info:
  name: 07 Update Demo Product
  type: http
  seq: 7

http:
  method: PUT
  url: "{{base_url}}/payment-products/:id"
  body:
    type: json
    data: |-
      {
        "name": "Bruno Demo Product Updated",
        "description": "Updated from Bruno.",
        "revenue_account_code": "4199",
        "revenue_account_name": "Other Education Revenue",
        "price": 150000,
        "status": "ACTIVE"
      }
  params:
    - name: id
      value: "{{payment_product_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
