info:
  name: 05 Find By ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/payment-policies/:id"
  params:
    - name: id
      value: "{{payment_policy_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
