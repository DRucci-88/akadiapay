info:
  name: 01 Login Reminder - Use SMAHB Token
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/auth/profile"
  auth:
    type: bearer
    token: "{{token_school_smahb}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
