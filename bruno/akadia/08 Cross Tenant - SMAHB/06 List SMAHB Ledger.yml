info:
  name: 06 List SMAHB Ledger
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/ledger-entries?page=1&size=50"
  params:
    - name: page
      value: "1"
      type: query
    - name: size
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
