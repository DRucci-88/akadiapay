info:
  name: 01 Budi Views Kevin Outstanding
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/students/:studentId/outstanding"
  params:
    - name: studentId
      value: "{{student_id_kevin}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
