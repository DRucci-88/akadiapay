info:
  name: 03 Student Kevin Views Own Outstanding
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/students/:studentId/outstanding"
  params:
    - name: studentId
      value: "{{student_id_kevin}}"
      type: path
  auth:
    type: bearer
    token: "{{token_student}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
