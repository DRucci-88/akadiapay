info:
  name: 09 Parent View Kevin Outstanding
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/students/{{student_id_kevin}}/outstanding"
  params:
    - name: studentId
      value: "{{student_id_kevin}}"
      type: path
  auth:
    type: bearer
    token: "{{token_parent}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
