info:
  name: 04 Filter Closed
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/student-obligations?status=CLOSED&page=1&size=50"
  params:
    - name: status
      value: "CLOSED"
      type: query
    - name: page
      value: "1"
      type: query
    - name: size
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
