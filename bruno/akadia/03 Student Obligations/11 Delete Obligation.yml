info:
  name: 11 Delete Obligation
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{base_url}}/student-obligations/:id"
  params:
    - name: id
      value: "{{student_obligation_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
