info:
  name: 07 Find By ID
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/student-obligations/:id"
  params:
    - name: id
      value: "{{student_obligation_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
