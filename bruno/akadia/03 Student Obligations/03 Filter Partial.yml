info:
  name: 03 Filter Partial
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/student-obligations?status=PARTIAL&page=1&size=50"
  params:
    - name: status
      value: "PARTIAL"
      type: query
    - name: page
      value: "1"
      type: query
    - name: size
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
