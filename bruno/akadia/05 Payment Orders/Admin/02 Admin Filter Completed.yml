info:
  name: 02 Admin Filter Completed
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/payment-orders?status=COMPLETED&page=1&size=50"
  params:
    - name: status
      value: "COMPLETED"
      type: query
    - name: page
      value: "1"
      type: query
    - name: size
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
