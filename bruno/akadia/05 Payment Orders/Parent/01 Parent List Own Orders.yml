info:
  name: 01 Parent List Own Orders
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/payment-orders?page=1&size=50"
  params:
    - name: page
      value: "1"
      type: query
    - name: size
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
