info:
  name: 03 Parent Find By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/payment-orders/:id"
  params:
    - name: id
      value: "{{payment_order_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
