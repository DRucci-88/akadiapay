info:
  name: 06 Parent Cancel Pending Order
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/payment-orders/:id/cancel"
  params:
    - name: id
      value: "{{payment_order_id_pending}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
