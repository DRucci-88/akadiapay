info:
  name: 03 Budi Cannot View Gilis Outstanding
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/students/:studentId/outstanding"
  params:
    - name: studentId
      value: "{{student_id_gilis}}"
      type: path
  auth:
    type: bearer
    token: "{{token_parent}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
