info:
  name: 04 Admin Get Allocations By Seed Order
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/payment-orders/:id/allocations"
  params:
    - name: id
      value: "{{payment_order_id_completed}}"
      type: path
  auth:
    type: bearer
    token: "{{token_school}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
