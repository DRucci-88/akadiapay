info:
  name: 02 Allocate Partial Payment - Study Tour
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/payment-orders/:id/allocate"
  body:
    type: json
    data: |-
      {
        "allocations": [
          {
            "student_obligation_id": "{{student_obligation_id_kevin_study_tour}}",
            "allocated_amount": 300000
          }
        ]
      }
  params:
    - name: id
      value: "{{payment_order_id_allocatable}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
