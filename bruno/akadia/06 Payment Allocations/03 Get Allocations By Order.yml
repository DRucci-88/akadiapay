info:
  name: 03 Get Allocations By Order
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/payment-orders/:id/allocations"
  params:
    - name: id
      value: "{{payment_order_id}}"
      type: path
  auth: inherit

runtime:
  variables:
    - name: payment_order_id
      value: 53055d12-abd9-49be-a037-e83dfff08176

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
