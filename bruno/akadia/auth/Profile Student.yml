info:
  name: Profile Student
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/auth/profile"
  auth:
    type: bearer
    token: "{{token_student}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
