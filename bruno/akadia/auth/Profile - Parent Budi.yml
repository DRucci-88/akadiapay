info:
  name: Profile - Parent Budi
  type: http
  seq: 22

http:
  method: GET
  url: "{{base_url}}/auth/profile"
  auth:
    type: bearer
    token: "{{token_parent}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
