info:
  name: Profile - Parent Asep
  type: http
  seq: 25

http:
  method: GET
  url: "{{base_url}}/auth/profile"
  auth:
    type: bearer
    token: "{{token_parent_smahb}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
